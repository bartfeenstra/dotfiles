#!/usr/bin/env bash

set -Eeuo pipefail

cd "$(dirname "$0")/.."

find ./bin | while read -r file; do
    if [ ! -f "$file" ]; then
        continue
    fi
    echo "Linting $file..."
    shellcheck -x "$file"
done